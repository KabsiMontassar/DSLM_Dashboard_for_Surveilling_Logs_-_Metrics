# DSLM Environment Configuration
# Sensitive and configurable values for the observability stack

# Grafana Configuration
GF_SECURITY_ADMIN_PASSWORD=admin
GF_SECURITY_ADMIN_USER=admin

# Alertmanager SMTP Configuration
ALERTMANAGER_SMTP_SMARTHOST=localhost:587
ALERTMANAGER_SMTP_FROM=alertmanager@example.org
ALERTMANAGER_SMTP_USERNAME=
ALERTMANAGER_SMTP_PASSWORD=

# Alertmanager Webhook Configuration
ALERTMANAGER_WEBHOOK_URL=http://127.0.0.1:5001/

# Service Ports (change if conflicts occur)
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
LOKI_PORT=3100
TEMPO_PORT=3200
TEMPO_OTLP_GRPC_PORT=4317
TEMPO_OTLP_HTTP_PORT=4318
ALERTMANAGER_PORT=9093
NODE_EXPORTER_PORT=9100
CADVISOR_PORT=8080

# Optional: External URLs for integrations
SLACK_WEBHOOK_URL=
PAGERDUTY_INTEGRATION_KEY=
EMAIL_RECIPIENTS=admin@example.com